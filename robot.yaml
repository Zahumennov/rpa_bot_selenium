tasks:
  Default:
    command: python -m robot --report NONE --outputdir output --logtitle "Task log" task.py

condaConfigFile: conda.yaml

artifactsDir: output

PATH:
  - .

PYTHONPATH:
  - .

ignoreFiles:
  - .gitignore